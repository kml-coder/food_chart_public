gunicorn
flask
flask-cors
recipe-scrapers
ingredient-parser-nlp
beautifulsoup4
requests

# The DeBERTa checkpoint was saved by transformers 5.6.0: its config.json uses
# the v5 "dtype" key and tokenizer_config.json declares "backend": "tokenizers".
# There is also no spm.model in the folder, so a v4 slow tokenizer cannot load it.
transformers==5.6.0
sentencepiece
huggingface-hub
